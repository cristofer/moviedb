$("#flash").html('<%= j render partial: "shared/notice_banner" %>')

$("#categories > *").empty();
$("#categories").html("<%= j categories %>")

$("#stars > *").empty();
$("#stars").html("<%= j stars %>")

$("#movies > *").empty();
$("#movies").html("<%= j render @movies %>")
